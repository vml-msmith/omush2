sudo: falsedist: preciselanguage: cppenv: GCC_VERSION=5 BUILD_TYPE=Debug CPP=11cache:  apt: trueaddons:  apt:    sources:    - boost-latest    - ubuntu-toolchain-r-test    packages:    - gcc-5    - libboost1.55-all-devcompiler:  - g++before_script:  - cd omush  - mkdir build  - cd build  - cmake ../.script: make